# Task 4 TDD 보고서

## RED

`npm run test:e2e -- --grep "진행|학생용 결과|업데이트" --reporter=line --workers=1`에서 진행 표시, 학생용 결과 목록, 모바일 도움말이 없어 실패했습니다.

## 구현

- 6단계 진행 표시와 쉬운 세 단계 설명, 핵심 용어 풀이, 보기의 화살표 뜻을 추가했습니다.
- 결과 화면을 다섯 미션의 의미 있는 카드 목록으로 분리했습니다.
- 모바일 도움말과 2026-07-18 학생 실사용 개선 기록을 추가했습니다.
- 기존 sceneGuide 기대값과 경로표 버튼명도 현재 학생용 문구로 갱신했습니다.

## GREEN

- `npm test`: 단위·콘텐츠·SSR 14개 통과
- `npm run build`: 통과
- `npm run test:e2e -- --grep "진행|학생용 결과|업데이트|axe|mobile" --reporter=line --workers=1`: 6개 집중 시나리오를 실행했습니다.

## 참고

전체 E2E 19개 실행은 시작 후 도구 세션 출력이 끊겨 최종 집계를 받지 못했습니다. 실패 artifact는 남지 않았습니다.

## 리뷰 수정

모바일 도움말 summary에 44px 터치 영역을 추가하고, 2026-07-17 접근성 보완 기록을 복원했습니다. DOM 라이브러리가 없는 타입 환경에서도 figure ref가 동작하도록 ref 타입을 `HTMLElement`로 조정했습니다.

## Task 5 안정화

거울 선택지는 학생용 상세 뜻풀이까지 접근 가능한 이름에 포함하므로, E2E 공통 완주 도우미는 설정 선택을 부분 이름으로 찾도록 조정했습니다. 꺼진 광원은 0구간일 때 `빛길이 시작되지 않음` 상태를 보인다는 회귀 검증을 유지했습니다. 관련 E2E 3개와 lint, typecheck가 통과했습니다.
